package client

var url = "https://127.0.0.1:1323" // TODO - this should not be hardcoded

var (
	fullname, email, groups            string
	groupName, groupDesc, groupMembers string
	manager, readonly, plainuser       bool
	userID, groupID                    uint32
)
